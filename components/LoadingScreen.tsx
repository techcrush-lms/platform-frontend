import React from 'react';
import LoadingSpinner from '@/components/ui/icons/LoadingIcon';

const LoadingScreen = () => (
  <div className='min-h-screen flex items-center justify-center'>
    <LoadingSpinner />
  </div>
);

export default LoadingScreen;
